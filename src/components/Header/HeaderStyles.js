import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  innerContainer: {
    display: "flex",
    alignItems: 'center',
    justifyContent: 'space-between',
    height: "80px",
  },
});
